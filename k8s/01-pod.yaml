# https://kubernetes.io/docs/concepts/workloads/pods/
apiVersion: v1
kind: Pod
metadata:
  name: "nginx"
  labels:
    app: "devops"
spec:
  containers:
  - name: "nginx"
    image: "nginx:alpine"